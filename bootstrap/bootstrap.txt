Bootstrap:
==========
It is a framework of css.
if we write code in bootstrap it will automatically be responsive.

getbootstap.com

Using bootstrap we can make web pages responsive
The website which works for all devices is responsive web design.

Bootstrap contains alot of predefined classes.

#your content should be in a column and column should be in a row and rows should be in a container.

container->row->column->content ---------> This is used in division and instead of float we take the col-md-3[12/4=3].

text alignment:
===============
text-start
text-center
text-end

navbar
Carousel----> move the images side by side in a page
cards
